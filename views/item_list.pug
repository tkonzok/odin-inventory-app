extends layout

block content
  h1= title

  ul
    each item in item_list
      li
        a(href=item.url) #{item.title}
        |  €#{item.price}
        |  Stock: #{item.numberInStock}

    else
      li There are no items.
